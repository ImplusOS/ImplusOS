# ImplusOS version X.X.X-Release

> Template for a release note. Copy this file, fill in the version and each
> section, and drop entries that don't apply. Keep the section order.

## Release note X.X.X-Release

## Breaking Changes
 - None as this is the first release.

## Added
 - None as this is the first release.

## Changed
 - None as this is the first release.

## Fixed
 - None as this is the first release.

## Removed
 - None as this is the first release.

## About ISO distribution
 - Primary distribution is the **x86_64** image
   (`Image/ImplusOS-x86_64-InstallMedia.iso`, built with `make image`).
   The LiveCD image (`make image_livecd`) is also published for quick QEMU
   testing.
 - The **arm64** image is not distributed yet (the AArch64 kernel does not
   currently link — see `Docs/Others/TODO_OS_Refactor.md` §8).
 - SHA-256 (x86_64 InstallMedia ISO): `XXXXX`
